---
const { headerItems } = Astro.props
---
<header class="w-full bg-white fixed top-0 left-0 right-0 z-10">
  <div class="py-5 w-full xl:w-[1440px] mx-auto">
    <nav class="flex justify-between items-center text-center">
      <a class="basis 1/3 text-3xl" href="/">Alejandro Arroyo</a>
      { headerItems === "true" ?      
        <div class=`flex-auto 2/3 flex justify-end items-center space-x-4 text-lg`>
          <a 
            href="/#recentWork" 
            class="cursor-pointer font-medium rounded-lg px-5 text-center  
            mr-2 transition-all duration-200 ease-in-out hover:scale-110 
            scale-90 opacity-70 hover:opacity-100 hover:text-[#792a15]"
          >TRABAJOS</a>
          <a 
            href="/#albums" 
            class="cursor-pointer font-medium rounded-lg px-5 text-center  
            mr-2 transition-all duration-200 ease-in-out hover:scale-110 
            scale-90 opacity-70 hover:opacity-100 hover:text-[#792a15]"
          >ÁLBUMES</a>
          <a 
            href="/#contact" 
            class="cursor-pointer font-medium rounded-lg px-5 text-center  
            mr-2 transition-all duration-200 ease-in-out hover:scale-110 
            scale-90 opacity-70 hover:opacity-100 hover:text-[#792a15]"
          >CONTACTO</a>
        </div>
        : ""
      }
    </nav>
  </div>
</header>

<!-- <div class="bg-gray-800 text-white">
  <nav class="container mx-auto px-4 py-3 flex items-center justify-between">
    <div class="text-lg font-bold">MyWebsite</div>
    <button id="menuButton" class="sm:hidden flex items-center text-white focus:outline-none">
      <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
      </svg>
    </button>
    <ul id="menu" class="hidden sm:flex sm:space-x-6 sm:items-center">
      <li><a href="#" class="hover:text-gray-300">Home</a></li>
      <li><a href="#" class="hover:text-gray-300">About</a></li>
      <li><a href="#" class="hover:text-gray-300">Services</a></li>
      <li><a href="#" class="hover:text-gray-300">Contact</a></li>
    </ul>
  </nav>

  <div id="mobileMenu" class="hidden sm:hidden bg-gray-700 px-4 py-2">
    <ul class="space-y-2">
      <li><a href="#" class="block text-gray-200 hover:text-gray-400">Home</a></li>
      <li><a href="#" class="block text-gray-200 hover:text-gray-400">About</a></li>
      <li><a href="#" class="block text-gray-200 hover:text-gray-400">Services</a></li>
      <li><a href="#" class="block text-gray-200 hover:text-gray-400">Contact</a></li>
    </ul>
  </div>
</div>

<script>
  const menuButton = document.getElementById('menuButton')
  const mobileMenu = document.getElementById('mobileMenu')

  if (menuButton && mobileMenu) {
    menuButton.addEventListener('click', () => {
      const isHidden = mobileMenu.classList.contains('hidden')
      if (isHidden) {
        mobileMenu.classList.remove('hidden')
      } else {
        mobileMenu.classList.add('hidden')
      }
    })
  }
</script> -->